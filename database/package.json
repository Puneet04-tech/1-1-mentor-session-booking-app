{
  "name": "mentor-database",
  "version": "1.0.0",
  "description": "Database migrations and seeds for Mentor Platform",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "setup": "npm run migrate && npm run seed",
    "reset": "node scripts/reset.js"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "dotenv": "^16.3.1"
  }
}
